linters:
  disable-all: true
  enable:
    # - staticcheck
    # - unused
    - golint

linters-settings:
  staticcheck:
    go: "1.24"

    checks: ["all"]

  unused:
    go: "1.24"

issues:
  include:
    - EXC0002
